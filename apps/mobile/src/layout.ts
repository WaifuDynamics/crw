export const MOBILE_WEB_WIDTH = 480;

/** Liquid glass dock metrics, shared by the tab bar and the screens it floats over. */
export const DOCK_HEIGHT = 64;
export const DOCK_INSET = 12;
export const DOCK_SIDE = 14;

/** Bottom padding a tab screen reserves so its last row clears the dock. */
export const DOCK_SPACE = DOCK_HEIGHT + DOCK_INSET + 18;
